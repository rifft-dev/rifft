[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "rifft-autogen"
version = "0.1.0"
description = "AutoGen adapter for Rifft"
requires-python = ">=3.9"
dependencies = ["rifft"]

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]

