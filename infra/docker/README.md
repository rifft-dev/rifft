# Docker

The root `docker-compose.yml` is the primary local entry point so `docker compose up -d` works exactly as documented.

Service Dockerfiles live with each app or package:

- `packages/collector/Dockerfile`
- `apps/api/Dockerfile`
- `apps/web/Dockerfile`

